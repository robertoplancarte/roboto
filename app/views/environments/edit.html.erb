<div class="flex flex-col min-h-screen bg-grey-lighter">
  <div class="container max-w-lg px-2 mx-auto">
    <div class="w-full px-6 py-8 text-black bg-white rounded shadow-md">
      <h1 class="mb-8 text-3xl text-center"> Editing Environment</h1>

      <%= render 'form', environment: @environment %>

      <%= link_to 'Show', @environment %> |
      <%= link_to 'Back', environments_path %>
    </div>
  </div>
</div>
